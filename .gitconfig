[alias]
	lg = log --color --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit
	co = checkout
	st = status
	ci = commit

# default one, must be before includes
[user]
	email = matthieu.jacquot.tech@protonmail.ch
	name = Matthieu Jacquot

[includeIf "gitdir:~/projects/pro/"]
    path = ~/projects/pro/.gitconfig

[includeIf "gitdir:~/projects/bcc/"]
    path = ~/projects/bcc/.gitconfig


